#include "types.h"

API int main(int argc, char** argv) {
    std::cout << "rTech : ";
    std::cout << VERSION_MAJOR << "." << VERSION_MINOR << "." << VERSION_PATCH << std::endl;

    
    return 0;
}